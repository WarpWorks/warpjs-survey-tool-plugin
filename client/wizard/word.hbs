<div class="modal fade" id="word-modal" tabindex="-1" role="dialog">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="word-preview-title">
        <button class="word-download">Download as word file</button>
        <span class="render-percent"></span>
        <span class="word-preview-title-text">Word File Preview</span>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="word-modal">
        <img src="../../assets/images/IIC-logo.png" height="40" width="97"/>
        <h2>IIC Project Explorer</h2>
        <h3 class="export-questionnaire-title">{{title}}</h3>
        <p>This summary shows how your project is rated in the main IPT categories. For each category, the average score is shown. In general, low score means few problems to be expected, high score means that in this category has a very high level of complexity and potential risks.</p>
        <h4 class="summary-label">Summary Project Assessment</h4>
        <div class="export-summary">
          {{#each values}}
            <div class="summary-scales">
              <h3 class="summary-category-title">{{category}}</h3>
              <div class="summary-content">
                <div class="summary-scale-container">
                  <div class="scale-section-one"></div>
                  <div class="scale-section-two"></div>
                  <div class="scale-section-three"></div>
                  <div class="scale-section-four"></div>
                  <div class="marker" data-score="{{answerAverage}}"></div>
                </div>
                <div class="average-number" data-score="{{answerAverage}}">
                  <p>{{answerAverage}}</p>
                </div>
              </div>
            </div>
          {{/each}}
        </div>
        <h4 class="summary-label">Summary Project Assessment</h4>
        <div class="export-details">
          {{#each details.data}}
            <div class="details-segment">
              <p class="summary-category">{{category}}</p>
             {{#if comments}}<p class="export-comments">Your comment: {{comments}}</p>{{/if}}
              {{#each iterations}}
                <div class="iteration-segment">
                  {{#if name}}<p class="summary-iteration">{{name}}</p>{{/if}}
                  {{#each questions}}
                    {{#if hasOptions}}
                      <div class="question-block">
                        <div class="position-display selected-position{{position}}">
                          <div class="position1"></div>
                          <div class="position2"></div>
                          <div class="position3"></div>
                          <div class="position4"></div>
                        </div>
                        <span class="summary-question">{{name}}</span>
                        <span class="summary-selection">{{option}}</span>
                      </div>
                      {{#if comments}}<p class="export-comments">Your comment: {{comments}}</p>{{/if}}
                    {{else}}
                      <p class="sub-category-name">{{name}}</p>
                    {{/if}}
                  {{/each}}
                </div>
              {{/each}}
            </div>
          {{/each}}
        </div>
      </div>
    </div>
  </div>
</div>
<div class="export-content">
  <img src="../../assets/images/IIC-logo.png" height="40px" width="97px"/>
  <h2>IIC Project Explorer</h2>
  <h3 class="export-questionnaire-title">{{title}}</h3>
  <p>This summary shows how your project is rated in the main IPT categories. For each category, the average score is shown. In general, low score means few problems to be expected, high score means that in this category has a very high level of complexity and potential risks.</p>
  <h4 class="summary-label">Summary Project Assessment</h4>
  <div class="export-summary-results"></div>
  <h4 class="summary-label">Summary Project Assessment</h4>
  <div class="export-details-results"></div>
</div>