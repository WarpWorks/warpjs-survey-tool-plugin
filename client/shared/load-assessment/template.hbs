<div class="survey-tool-load-assessment-popup">
  <div class="export-properties">
    <div class="export-title">Importing ‘{{projectName}}’ from PC to browser:</div>
    <div class="export-meta">
      <div class="export-revision">Version: {{assessmentMeta.exportRevision}}</div>
      <div class="export-name">Created by: {{assessmentMeta.exportName}}</div>
      <div class="export-description">Description of this version: {{assessmentMeta.exportDescription}}</div>
    </div>
  </div>

  <div class="survey-tool-load-assessment-popup-message">{{message}}</div>
  <div class="btn-group">
    {{#each buttons as |button|}}
      <button type="button" class="btn btn-{{button.btnClass}}"
        data-survey-tool-action="{{button.action}}"
        >{{button.label}}</button>
    {{/each}}
  </div>
</div>
